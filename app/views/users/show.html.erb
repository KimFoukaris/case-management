<%= render 'notices' %>

<div class="center">

  <div class="info">
    <p><strong>Name:</strong> <%= @user.name %></p>
    <p><strong>Job Title:</strong> <%= @user.title %></p>
    <p><%= link_to 'Edit User Profile', edit_user_path(@user) %></p>
  </div>
</div>
